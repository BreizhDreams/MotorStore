{% block body %}
<h1>Tous nos Produits ! </h1>
<div class="row">
    {% for productVO in productVOs %}
    <div class="col-md-4">
        <div class="itemProduct">
            <div class="imageProductItem">
                <img src="{{productVO.photoURL}}" alt="" class="img-fluid rounded">
            </div>
            <h5>{{productVO.designation}}</h5>
            <p>{{productVO.brandVO}}</p>
            <span class="productPrice">
                <button type="button" class="btn">Ajouter au Panier</button>
                <p>{{(productVO.prixTTC / 100)|number_format(2,',','.')}} €</p>
            </span>
        </div>
    </div>
    {% endfor %}
</div>


{% endblock %}